# Release Dashboard Application - Production Dependencies

# Core Python modules (using standard library, minimal external dependencies)
# csv_to_json module is part of the project, not external

# Configuration management
python-dotenv>=0.19.0

# CSV-to-JSON conversion uses Python standard library (csv, json)
# Dashboard uses Plotly.js (JavaScript CDN, no Python package needed)

# Informe PPT de Postmortem por Release (converters/src/report_generator/)
python-pptx>=0.6.23
plotly>=5.20.0
kaleido>=0.2.1
